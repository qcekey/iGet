# 📤 Пошаговая инструкция: Загрузка проекта на GitHub

## ✅ Ваша информация
- **GitHub username:** `qcekey`
- **GitHub привязан к Cursor:** ✅ (авторизация автоматическая)
- **Проект:** iGet

---

## 📋 Шаг 1: Инициализация Git репозитория

Откройте терминал в Cursor (или PowerShell) и выполните:

```powershell
# Перейдите в папку проекта (если еще не там)
cd C:\Users\FreakNick\Desktop\iGet

# Инициализируйте Git репозиторий
git init
```

**Ожидаемый результат:** `Initialized empty Git repository in C:\Users\FreakNick\Desktop\iGet\.git`

---

## 📋 Шаг 2: Проверка .gitignore

Убедитесь, что чувствительные файлы не будут загружены:

```powershell
# Проверьте статус (должны увидеть много файлов, но НЕ .env и keyserver)
git status
```

**Важно:** В списке НЕ должно быть:
- ❌ `.env`
- ❌ `keyserver` и `keyserver.pub`
- ❌ `data/*.session`
- ❌ `data/*.db`
- ❌ `data/settings.json`

Если эти файлы видны в `git status` - проверьте `.gitignore` файл.

---

## 📋 Шаг 3: Добавление файлов в Git

```powershell
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что будет закоммичено
git status
```

**Ожидаемый результат:** Вы увидите список файлов, готовых к коммиту. `.env` и `keyserver` НЕ должны быть в списке.

---

## 📋 Шаг 4: Первый коммит

```powershell
# Сделайте первый коммит
git commit -m "Initial commit: iGet vacancy monitoring system"
```

**Ожидаемый результат:** 
```
[main (root-commit) xxxxxxx] Initial commit: iGet vacancy monitoring system
 X files changed, Y insertions(+)
```

---

## 📋 Шаг 5: Создание репозитория на GitHub

1. **Откройте браузер** и перейдите на https://github.com
2. **Войдите** в свой аккаунт (qcekey)
3. **Нажмите** на иконку **"+"** в правом верхнем углу
4. **Выберите** **"New repository"**
5. **Заполните форму:**
   - **Repository name:** `iGet`
   - **Description:** `AI-powered vacancy monitoring and filtering system`
   - **Visibility:** 
     - ✅ **Public** (если хотите открытый репозиторий)
     - ✅ **Private** (если хотите приватный)
   - ⚠️ **НЕ отмечайте** галочки:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   
   (Эти файлы уже есть в проекте!)
6. **Нажмите** **"Create repository"**

---

## 📋 Шаг 6: Подключение к GitHub

После создания репозитория GitHub покажет страницу с инструкциями. Выполните в терминале:

```powershell
# Добавьте GitHub репозиторий как remote
git remote add origin https://github.com/qcekey/iGet.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Проверьте, что remote добавлен
git remote -v
```

**Ожидаемый результат:**
```
origin  https://github.com/qcekey/iGet.git (fetch)
origin  https://github.com/qcekey/iGet.git (push)
```

---

## 📋 Шаг 7: Загрузка кода на GitHub

```powershell
# Загрузите код на GitHub
git push -u origin main
```

**Если GitHub привязан к Cursor:**
- Авторизация должна произойти автоматически
- Может появиться окно браузера для подтверждения (один раз)

**Ожидаемый результат:**
```
Enumerating objects: X, done.
Counting objects: 100% (X/X), done.
...
To https://github.com/qcekey/iGet.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

---

## 📋 Шаг 8: Проверка на GitHub

1. **Откройте** https://github.com/qcekey/iGet
2. **Проверьте**, что:
   - ✅ Все файлы загружены
   - ✅ README.md отображается правильно
   - ✅ `.env` файл **НЕ виден** на GitHub
   - ✅ `keyserver` файлы **НЕ видны** на GitHub
   - ✅ Папка `data/` **НЕ видна** на GitHub

---

## 🎉 Готово!

Ваш проект теперь на GitHub! 🚀

**Ссылка на репозиторий:** https://github.com/qcekey/iGet

---

## ⚠️ Если что-то пошло не так

### Проблема: "remote origin already exists"

```powershell
# Удалите существующий remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/qcekey/iGet.git
```

### Проблема: "Authentication failed"

Если Cursor не авторизует автоматически:

1. **Создайте Personal Access Token:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: "iGet project"
   - Scope: отметьте `repo`
   - Нажмите "Generate token"
   - **Скопируйте токен** (показывается только один раз!)

2. **Используйте токен при push:**
   ```powershell
   # При запросе пароля:
   Username: qcekey
   Password: ваш_персональный_токен
   ```

### Проблема: "branch 'main' does not exist"

```powershell
# Создайте ветку main
git checkout -b main

# Или переименуйте текущую
git branch -M main
```

### Проблема: Случайно закоммитили .env

```powershell
# Удалите из Git (но оставьте локально)
git rm --cached .env

# Сделайте новый коммит
git commit -m "Remove .env from repository"

# Загрузите изменения
git push
```

---

## 📝 Дополнительно

После успешной загрузки вы можете:

1. **Обновить описание репозитория** на GitHub
2. **Добавить теги** (topics) для лучшей индексации:
   - `python`
   - `fastapi`
   - `telegram`
   - `vacancy-parser`
   - `ai-filtering`
3. **Создать Issues** для отслеживания задач
4. **Настроить GitHub Actions** для CI/CD (опционально)

---

**Удачи! 🎉**
