# 🚀 Инструкция по загрузке проекта на GitHub

Этот файл содержит пошаговую инструкцию по загрузке проекта iGet на GitHub.

## ✅ Подготовка завершена

Следующие файлы были созданы/обновлены для GitHub:
- ✅ `.gitignore` - обновлен, исключает все чувствительные данные
- ✅ `.env.example` - пример конфигурации
- ✅ `README.md` - улучшен для GitHub
- ✅ `CONTRIBUTING.md` - руководство для разработчиков
- ✅ `LICENSE` - MIT лицензия

## 📋 Шаги для загрузки на GitHub

### Шаг 1: Инициализация Git репозитория

Откройте PowerShell в папке проекта и выполните:

```powershell
cd C:\Users\FreakNick\Desktop\iGet

# Инициализируйте git репозиторий
git init

# Проверьте, что .gitignore работает правильно
git status
```

**Важно:** Убедитесь, что следующие файлы НЕ отображаются в `git status`:
- `.env`
- `keyserver` и `keyserver.pub`
- `data/*.session`
- `data/*.db`
- `data/settings.json`

### Шаг 2: Первый коммит

```powershell
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что будет закоммичено
git status

# Сделайте первый коммит
git commit -m "Initial commit: iGet vacancy monitoring system"
```

### Шаг 3: Создание репозитория на GitHub

1. Откройте https://github.com в браузере
2. Войдите в свой аккаунт
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name:** `iGet` (или другое имя)
   - **Description:** `AI-powered vacancy monitoring and filtering system`
   - **Visibility:** Выберите Public или Private
   - **НЕ создавайте** README, .gitignore или LICENSE (они уже есть)
5. Нажмите **"Create repository"**

### Шаг 4: Подключение к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```powershell
# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/iGet.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Если потребуется авторизация:**

GitHub больше не поддерживает пароли для Git операций. Используйте один из вариантов:

#### Вариант A: Personal Access Token (рекомендуется)

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте имя токену (например, "iGet project")
4. Выберите срок действия
5. Отметьте scope: **`repo`** (полный доступ к репозиториям)
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (он показывается только один раз!)
8. При `git push` используйте токен вместо пароля:
   ```
   Username: ваш_github_username
   Password: ваш_персональный_токен
   ```

#### Вариант B: SSH ключи

1. Сгенерируйте SSH ключ (если еще нет):
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
2. Добавьте ключ в GitHub: https://github.com/settings/keys
3. Используйте SSH URL:
   ```powershell
   git remote set-url origin git@github.com:YOUR_USERNAME/iGet.git
   ```

### Шаг 5: Проверка

После загрузки:

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. **ВАЖНО:** Проверьте, что `.env`, `keyserver`, и другие чувствительные файлы НЕ видны на GitHub

### Шаг 6: Обновление README (опционально)

После загрузки обновите ссылки в `README.md`:
- Замените `YOUR_USERNAME` на ваш реальный GitHub username
- Обновите ссылки на Issues и Pull Requests

## 🔒 Безопасность

### Если случайно загрузили чувствительные данные:

Если вы случайно закоммитили `.env` или другие чувствительные файлы:

1. **Немедленно удалите файл из истории:**
   ```powershell
   git rm --cached .env
   git commit -m "Remove sensitive .env file"
   git push
   ```

2. **Если данные уже были загружены:**
   - Смените все API ключи и пароли
   - Рассмотрите использование `git-filter-repo` для полной очистки истории

### Проверка перед каждым коммитом:

```powershell
# Проверьте, что чувствительные файлы игнорируются
git check-ignore .env
git check-ignore keyserver
git check-ignore data/*.session

# Если команда ничего не выводит - файл НЕ игнорируется!
```

## 📝 Дополнительные настройки (опционально)

### GitHub Actions для CI/CD

Создайте `.github/workflows/ci.yml`:

```yaml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      - run: pip install -r requirements_parsers.txt
      - run: python -m pytest tests/
```

### GitHub Pages для документации

1. Перейдите в Settings → Pages
2. Выберите source: `main` branch, `/docs` folder
3. Документация будет доступна по адресу: `https://YOUR_USERNAME.github.io/iGet/`

## 🎉 Готово!

Ваш проект теперь на GitHub и готов к совместной разработке!

## 📚 Полезные ссылки

- [GitHub Docs](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- [GitHub Flow](https://guides.github.com/introduction/flow/)

---

**Вопросы?** Создайте Issue в репозитории!
