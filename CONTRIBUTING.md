# Руководство по участию в разработке iGet

Спасибо за интерес к проекту iGet! Мы рады любому вкладу в развитие проекта.

## 🚀 Как начать

1. **Fork репозиторий** на GitHub
2. **Клонируйте** ваш fork:
   ```bash
   git clone https://github.com/YOUR_USERNAME/iGet.git
   cd iGet
   ```
3. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```
4. **Установите зависимости:**
   ```bash
   pip install -r requirements_parsers.txt
   ```
5. **Создайте ветку** для ваших изменений:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📝 Процесс разработки

### Стиль кода

- Следуйте [PEP 8](https://www.python.org/dev/peps/pep-0008/) для Python кода
- Используйте type hints где возможно
- Пишите docstrings для функций и классов
- Максимальная длина строки: 100 символов

### Коммиты

Используйте понятные сообщения коммитов:

```bash
# Хорошо
git commit -m "feat: добавлен парсер для Habr"
git commit -m "fix: исправлена обработка ошибок в HeadHunter парсере"
git commit -m "docs: обновлена документация по установке"

# Плохо
git commit -m "fix"
git commit -m "изменения"
```

Префиксы:
- `feat:` — новая функция
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование кода
- `refactor:` — рефакторинг
- `test:` — добавление тестов
- `chore:` — обновление зависимостей, конфигурации

### Тестирование

Перед созданием Pull Request убедитесь, что:

1. Все существующие тесты проходят:
   ```bash
   python -m pytest tests/
   ```

2. Ваш код не ломает существующую функциональность

3. Добавлены тесты для новой функциональности (если применимо)

## 🔀 Создание Pull Request

1. **Убедитесь, что ваш код готов:**
   - Код следует стилю проекта
   - Все тесты проходят
   - Документация обновлена (если нужно)

2. **Синхронизируйте с основным репозиторием:**
   ```bash
   git remote add upstream https://github.com/ORIGINAL_OWNER/iGet.git
   git fetch upstream
   git rebase upstream/main
   ```

3. **Отправьте изменения:**
   ```bash
   git push origin feature/your-feature-name
   ```

4. **Создайте Pull Request** на GitHub

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Новая функция
- [ ] Исправление бага
- [ ] Обновление документации
- [ ] Рефакторинг

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Добавлены/обновлены тесты
- [ ] Документация обновлена
- [ ] Все тесты проходят
```

## 🐛 Сообщение о багах

При создании Issue о баге, пожалуйста, укажите:

1. **Описание проблемы** — что происходит
2. **Ожидаемое поведение** — что должно происходить
3. **Шаги для воспроизведения**
4. **Окружение:**
   - ОС и версия
   - Python версия
   - Версия зависимостей

## 💡 Предложения новых функций

Мы приветствуем предложения новых функций! При создании Issue:

1. Опишите проблему, которую решает функция
2. Предложите решение
3. Обсудите альтернативные подходы (если есть)

## 📚 Области для вклада

### Код
- Новые парсеры для других источников вакансий
- Улучшение существующих парсеров
- Оптимизация производительности
- Исправление багов

### Документация
- Улучшение README
- Добавление примеров использования
- Перевод документации на другие языки
- Видео-туториалы

### Тесты
- Увеличение покрытия тестами
- Интеграционные тесты
- Тесты производительности

### UI/UX
- Улучшение веб-интерфейса
- Адаптивный дизайн
- Новые функции в UI

## ❓ Вопросы?

Если у вас есть вопросы, создайте [Issue](https://github.com/YOUR_USERNAME/iGet/issues) с меткой `question`.

## 📜 Кодекс поведения

- Будьте уважительны к другим участникам
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества

---

Спасибо за ваш вклад! 🎉
