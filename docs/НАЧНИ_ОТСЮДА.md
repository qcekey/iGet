# 🎯 НАЧНИ ОТСЮДА - Развертывание iGet

## ⚡ Быстрый способ (рекомендуется)

### Шаг 1: Откройте терминал

**Windows:**
- Откройте PowerShell или Git Bash
- Или используйте WSL (Windows Subsystem for Linux)

### Шаг 2: Подключитесь к серверу

```bash
ssh root@85.198.84.197
```

Пароль: `QV%LQ&dzXi9&`

### Шаг 3: Выполните команды на сервере

Скопируйте и выполните команды из файла **`deploy_commands.txt`** или следуйте инструкциям в **`БЫСТРОЕ_РАЗВЕРТЫВАНИЕ.md`**

---

## 📚 Все доступные инструкции

1. **`БЫСТРОЕ_РАЗВЕРТЫВАНИЕ.md`** ⭐ - Начните отсюда! Пошаговая инструкция
2. **`РАЗВЕРТЫВАНИЕ.md`** - Подробная документация на русском
3. **`ЗАПУСК_С_СЕРВЕРА.md`** - Как управлять приложением после развертывания
4. **`deploy_commands.txt`** - Все команды в одном файле (для копирования)
5. **`deploy.sh`** - Автоматический скрипт (для Linux/Mac/WSL/Git Bash)
6. **`deploy.ps1`** - PowerShell скрипт (показывает инструкции)

---

## 🚀 Автоматическое развертывание

### Если у вас есть WSL или Git Bash:

```bash
# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите развертывание
bash deploy.sh
```

Скрипт автоматически:
- ✅ Установит все зависимости
- ✅ Загрузит проект на сервер
- ✅ Настроит Python окружение
- ✅ Создаст systemd сервис
- ✅ Настроит Nginx
- ✅ Запустит приложение

---

## 📋 Ручное развертывание (пошагово)

### 1. Подключение к серверу

```bash
ssh root@85.198.84.197
# Пароль: QV%LQ&dzXi9&
```

### 2. Установка зависимостей

```bash
apt update && apt upgrade -y
apt install -y python3 python3-pip python3-venv git curl wget nginx

# Chrome для LinkedIn парсера
wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
apt update && apt install -y google-chrome-stable
```

### 3. Создание директории

```bash
mkdir -p /opt/iget/data
```

### 4. Загрузка файлов (с вашего компьютера, в новом терминале)

```bash
# PowerShell
scp -r C:\Users\FreakNick\Desktop\iget-data\* root@85.198.84.197:/opt/iget/

# Или Git Bash/WSL
rsync -avz --exclude 'venv' --exclude '__pycache__' \
  C:/Users/FreakNick/Desktop/iget-data/ root@85.198.84.197:/opt/iget/
```

### 5. Настройка Python (на сервере)

```bash
cd /opt/iget
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements_parsers.txt
pip install fastapi uvicorn aiohttp selenium webdriver-manager beautifulsoup4
```

### 6. Создание systemd сервиса

```bash
cat > /etc/systemd/system/iget.service << 'EOF'
[Unit]
Description=iGet Application
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/iget
Environment="PATH=/opt/iget/venv/bin"
ExecStart=/opt/iget/venv/bin/python /opt/iget/start_iget.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable iget
```

### 7. Настройка Nginx

```bash
cat > /etc/nginx/sites-available/iget << 'EOF'
server {
    listen 80;
    server_name 85.198.84.197;
    client_max_body_size 50M;
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
EOF

ln -sf /etc/nginx/sites-available/iget /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl restart nginx
```

### 8. Настройка Firewall

```bash
ufw allow 80/tcp
ufw allow 8000/tcp
ufw --force enable || true
```

### 9. Запуск приложения

```bash
systemctl start iget
systemctl status iget
```

---

## ✅ Проверка работы

После выполнения всех шагов:

1. **Проверьте статус:**
   ```bash
   systemctl status iget
   ```

2. **Откройте в браузере:**
   - http://85.198.84.197
   - http://85.198.84.197:8000

---

## 🔧 Управление приложением

```bash
# Подключение к серверу
ssh root@85.198.84.197

# Управление
systemctl start iget      # Запуск
systemctl stop iget       # Остановка
systemctl restart iget    # Перезапуск
systemctl status iget     # Статус

# Логи
journalctl -u iget -f    # В реальном времени
journalctl -u iget -n 50 # Последние 50 строк
```

---

## 🐛 Проблемы?

1. **Приложение не запускается:**
   ```bash
   journalctl -u iget -n 100
   ```

2. **Не могу подключиться:**
   ```bash
   systemctl status iget
   netstat -tlnp | grep 8000
   ufw status
   ```

3. **См. подробности:** `РАЗВЕРТЫВАНИЕ.md` (раздел "Решение проблем")

---

## 📞 Дополнительная информация

- **Подробная инструкция:** `РАЗВЕРТЫВАНИЕ.md`
- **Управление после развертывания:** `ЗАПУСК_С_СЕРВЕРА.md`
- **Все команды:** `deploy_commands.txt`

---

## 🎉 Готово!

После развертывания приложение будет доступно по адресу:
**http://85.198.84.197**

Приложение автоматически запускается при перезагрузке сервера.
