# ⚡ Быстрое развертывание iGet на сервере

## 🎯 Цель
Развернуть проект на сервере 85.198.84.197 и запустить его.

---

## 📋 Шаг 1: Подключение к серверу

Откройте терминал (PowerShell, Git Bash или WSL) и выполните:

```bash
ssh root@85.198.84.197
```

При запросе пароля введите: `QV%LQ&dzXi9&`

---

## 📋 Шаг 2: Установка зависимостей на сервере

После подключения выполните на сервере:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
apt install -y python3 python3-pip python3-venv git curl wget nginx

# Установка Chrome (для LinkedIn парсера)
wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
apt update
apt install -y google-chrome-stable
```

---

## 📋 Шаг 3: Создание директории проекта

```bash
mkdir -p /opt/iget
mkdir -p /opt/iget/data
```

---

## 📋 Шаг 4: Загрузка файлов проекта

**Откройте НОВЫЙ терминал на вашем компьютере** (не закрывая SSH сессию) и выполните:

### Вариант A: Через SCP (PowerShell)

```powershell
scp -r C:\Users\FreakNick\Desktop\iget-data\* root@85.198.84.197:/opt/iget/
```

### Вариант B: Через rsync (Git Bash или WSL)

```bash
rsync -avz --progress --exclude 'venv' --exclude '__pycache__' --exclude '*.pyc' \
  C:/Users/FreakNick/Desktop/iget-data/ root@85.198.84.197:/opt/iget/
```

**Важно:** Исключите папку `venv` - она будет создана на сервере.

---

## 📋 Шаг 5: Настройка Python окружения

**Вернитесь в SSH сессию на сервере** и выполните:

```bash
cd /opt/iget

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements_parsers.txt
pip install fastapi uvicorn aiohttp selenium webdriver-manager beautifulsoup4

# Если iget нужно скопировать из локальной установки
# (только если вы загрузили папку venv)
if [ -d "venv/Lib/site-packages/iget" ]; then
    cp -r venv/Lib/site-packages/iget* venv/lib/python*/site-packages/ 2>/dev/null || true
fi
```

---

## 📋 Шаг 6: Создание systemd сервиса

```bash
cat > /etc/systemd/system/iget.service << 'EOF'
[Unit]
Description=iGet Application
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/iget
Environment="PATH=/opt/iget/venv/bin"
ExecStart=/opt/iget/venv/bin/python /opt/iget/start_iget.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable iget
```

---

## 📋 Шаг 7: Настройка Nginx

```bash
cat > /etc/nginx/sites-available/iget << 'EOF'
server {
    listen 80;
    server_name 85.198.84.197;

    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
EOF

ln -sf /etc/nginx/sites-available/iget /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx
```

---

## 📋 Шаг 8: Настройка Firewall

```bash
ufw allow 80/tcp
ufw allow 8000/tcp
ufw --force enable || true
```

---

## 📋 Шаг 9: Запуск приложения

```bash
# Запуск сервиса
systemctl start iget

# Проверка статуса
systemctl status iget

# Просмотр логов (в реальном времени)
journalctl -u iget -f
```

Для выхода из просмотра логов нажмите `Ctrl+C`.

---

## ✅ Проверка работы

1. **Проверьте статус сервиса:**
   ```bash
   systemctl status iget
   ```

2. **Проверьте, что порт открыт:**
   ```bash
   netstat -tlnp | grep 8000
   ```

3. **Откройте в браузере:**
   - http://85.198.84.197 (через Nginx)
   - http://85.198.84.197:8000 (напрямую)

---

## 🚀 Управление приложением

### Запуск/остановка

```bash
# Запуск
systemctl start iget

# Остановка
systemctl stop iget

# Перезапуск
systemctl restart iget

# Статус
systemctl status iget
```

### Просмотр логов

```bash
# В реальном времени
journalctl -u iget -f

# Последние 50 строк
journalctl -u iget -n 50

# Только ошибки
journalctl -u iget -p err
```

---

## 🐛 Если что-то пошло не так

### Приложение не запускается

```bash
# Проверьте логи
journalctl -u iget -n 100

# Проверьте зависимости
cd /opt/iget
source venv/bin/activate
python -c "import iget; print('OK')"
```

### Не могу подключиться из браузера

```bash
# Проверьте статус
systemctl status iget
systemctl status nginx

# Проверьте порты
netstat -tlnp | grep 8000

# Проверьте firewall
ufw status
```

---

## 📝 Дополнительная информация

- **Подробная инструкция:** см. `РАЗВЕРТЫВАНИЕ.md`
- **Инструкция по запуску:** см. `ЗАПУСК_С_СЕРВЕРА.md`
- **Все команды в одном файле:** см. `deploy_commands.txt`

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет доступно по адресу:
**http://85.198.84.197**

Приложение будет автоматически запускаться при перезагрузке сервера.
