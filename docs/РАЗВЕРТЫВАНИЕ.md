# 🚀 Развертывание iGet на сервере

## Быстрый старт

### Вариант 1: Автоматическое развертывание (рекомендуется)

1. **Убедитесь, что у вас установлен rsync** (обычно уже есть в Linux/Mac, для Windows можно использовать Git Bash или WSL)

2. **Сделайте скрипт исполняемым и запустите:**
   ```bash
   # Linux/Mac
   chmod +x deploy.sh
   ./deploy.sh
   
   # Windows (Git Bash или WSL)
   chmod +x deploy.sh
   bash deploy.sh
   ```

Скрипт автоматически:
- ✅ Установит все зависимости
- ✅ Загрузит проект на сервер
- ✅ Настроит Python окружение
- ✅ Создаст systemd сервис
- ✅ Настроит Nginx
- ✅ Откроет порты в firewall
- ✅ Запустит приложение

### Вариант 2: Ручное развертывание

Следуйте подробной инструкции в файле `DEPLOYMENT.md` (на английском) или выполните шаги ниже.

---

## 📋 Ручная установка (пошагово)

### 1. Подключение к серверу

```bash
ssh root@85.198.84.197
# Пароль: QV%LQ&dzXi9&
```

### 2. Установка зависимостей

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и инструментов
apt install -y python3 python3-pip python3-venv git curl wget nginx

# Установка Chrome (для LinkedIn парсера)
wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
apt update
apt install -y google-chrome-stable
```

### 3. Загрузка проекта

**С вашего компьютера** (в новом терминале):

```bash
# Windows PowerShell
scp -r C:\Users\FreakNick\Desktop\iget-data\* root@85.198.84.197:/opt/iget/

# Или через rsync (лучше, исключает ненужные файлы)
rsync -avz --exclude 'venv' --exclude '__pycache__' --exclude '*.pyc' \
  C:\Users\FreakNick\Desktop\iget-data\ root@85.198.84.197:/opt/iget/
```

**На сервере:**

```bash
mkdir -p /opt/iget
cd /opt/iget
```

### 4. Настройка Python

```bash
cd /opt/iget
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements_parsers.txt
pip install fastapi uvicorn  # если не установлены
```

### 5. Создание systemd сервиса

```bash
cat > /etc/systemd/system/iget.service << 'EOF'
[Unit]
Description=iGet Application
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/iget
Environment="PATH=/opt/iget/venv/bin"
ExecStart=/opt/iget/venv/bin/python /opt/iget/start_iget.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable iget
systemctl start iget
```

### 6. Настройка Nginx

```bash
cat > /etc/nginx/sites-available/iget << 'EOF'
server {
    listen 80;
    server_name 85.198.84.197;

    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
EOF

ln -s /etc/nginx/sites-available/iget /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t
systemctl restart nginx
```

### 7. Открытие портов

```bash
ufw allow 80/tcp
ufw allow 8000/tcp
ufw --force enable
```

### 8. Проверка

```bash
# Проверка статуса
systemctl status iget

# Просмотр логов
journalctl -u iget -f
```

---

## 🌐 Доступ к приложению

После развертывания приложение будет доступно:

- **Через Nginx:** http://85.198.84.197
- **Напрямую:** http://85.198.84.197:8000

---

## 🔧 Управление приложением

### Полезные команды

```bash
# Запуск
systemctl start iget

# Остановка
systemctl stop iget

# Перезапуск
systemctl restart iget

# Статус
systemctl status iget

# Логи (последние 50 строк)
journalctl -u iget -n 50

# Логи в реальном времени
journalctl -u iget -f
```

### Обновление приложения

```bash
# 1. Остановите сервис
systemctl stop iget

# 2. Загрузите новые файлы (с вашего компьютера)
rsync -avz --exclude 'venv' --exclude '__pycache__' \
  C:\Users\FreakNick\Desktop\iget-data\ root@85.198.84.197:/opt/iget/

# 3. На сервере обновите зависимости (если нужно)
cd /opt/iget
source venv/bin/activate
pip install -r requirements_parsers.txt --upgrade

# 4. Запустите сервис
systemctl start iget
```

---

## 🐛 Решение проблем

### Приложение не запускается

```bash
# Проверьте логи
journalctl -u iget -n 100

# Проверьте, что все зависимости установлены
cd /opt/iget
source venv/bin/activate
python -c "import iget; print('OK')"
```

### Не могу подключиться извне

```bash
# Проверьте, что приложение запущено
systemctl status iget
curl http://localhost:8000

# Проверьте firewall
ufw status

# Проверьте, что порт открыт
netstat -tlnp | grep 8000
```

### LinkedIn парсер не работает

```bash
# Проверьте установку Chrome
google-chrome --version

# Для headless режима может потребоваться
apt install -y xvfb
```

---

## 📝 Важные замечания

1. **Безопасность:** Рекомендуется создать отдельного пользователя вместо root для запуска приложения
2. **SSL:** Для продакшена рекомендуется настроить SSL сертификат (Let's Encrypt)
3. **Резервное копирование:** Регулярно делайте бэкап папки `/opt/iget/data`
4. **Мониторинг:** Настройте мониторинг ресурсов сервера (CPU, память, диск)

---

## ✅ Чек-лист

- [ ] Подключение к серверу работает
- [ ] Все зависимости установлены
- [ ] Проект загружен на сервер
- [ ] Python окружение настроено
- [ ] Systemd сервис создан и запущен
- [ ] Nginx настроен
- [ ] Порты открыты в firewall
- [ ] Приложение доступно извне
- [ ] Логи показывают успешный запуск

---

## 🎉 Готово!

Ваше приложение развернуто и готово к использованию!

Для подробной документации см. `DEPLOYMENT.md`
