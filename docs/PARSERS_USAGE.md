# Инструкция по использованию парсеров HeadHunter и LinkedIn

## ✅ Текущий статус

- **HeadHunter**: ✅ Работает через официальный API
- **LinkedIn**: ⚠️ Требует Selenium (может работать медленно)

## 🚀 Быстрый старт

### 1. Включение HeadHunter

1. Откройте веб-интерфейс приложения
2. В разделе "BASIC SEARCH" найдите переключатель "Enable HeadHunter"
3. Включите его
4. Укажите поисковый запрос (например: "Python разработчик")
5. Выберите регион (1 = Москва, 2 = СПб, 113 = Россия)
6. Укажите количество страниц (1-20)
7. Нажмите "Save Settings"

### 2. Включение LinkedIn

1. Включите переключатель "Enable LinkedIn"
2. Укажите поисковый запрос
3. Укажите локацию (например: "Moscow, Russia")
4. **Рекомендуется**: укажите email и password для авторизации
5. Нажмите "Save Settings"

⚠️ **Важно**: LinkedIn парсинг требует:
- Установленный Chrome браузер
- Selenium (устанавливается автоматически)
- Может работать медленно (открывает браузер)

## 🔍 Проверка работоспособности

Запустите диагностический скрипт:

```bash
python check_parsers.py
```

Этот скрипт проверит:
- Доступность API HeadHunter
- Установку Selenium для LinkedIn
- Настройки парсеров
- Сохранение вакансий

## 📊 Как это работает

### HeadHunter

1. Использует официальный API: `https://api.hh.ru/vacancies`
2. Не требует авторизации
3. Быстрый и надежный
4. Получает до 100 вакансий на страницу
5. Автоматически фильтрует по дате публикации

### LinkedIn

1. Использует Selenium для открытия браузера
2. Может требовать авторизацию
3. Работает медленнее (нужно время на загрузку страниц)
4. Может быть заблокирован LinkedIn при частых запросах

## 🐛 Решение проблем

### HeadHunter не работает

1. **Проверьте интернет-соединение**
   ```bash
   # Откройте в браузере:
   https://api.hh.ru/vacancies?text=Python&area=1&per_page=1
   ```

2. **Проверьте настройки**
   - Убедитесь, что `enable_headhunter: true` в `data/settings.json`
   - Проверьте, что указан поисковый запрос

3. **Проверьте логи**
   - Запустите приложение и посмотрите консоль
   - Ищите сообщения с префиксом "HeadHunter:"

### LinkedIn не работает

1. **Убедитесь, что Chrome установлен**
   - Chrome должен быть установлен в системе
   - ChromeDriver установится автоматически

2. **Установите зависимости**
   ```bash
   pip install selenium webdriver-manager
   ```

3. **Проверьте авторизацию**
   - LinkedIn может требовать авторизацию
   - Укажите email и password в настройках

4. **Проверьте логи**
   - Ищите сообщения с префиксом "LinkedIn:"

### Вакансии не сохраняются

1. **Проверьте файл `data/vacancies.json`**
   ```bash
   # Посмотрите содержимое файла
   cat data/vacancies.json
   ```

2. **Проверьте права доступа**
   - Убедитесь, что у приложения есть права на запись в `data/`

3. **Запустите тест сохранения**
   ```bash
   python test_parsers_integration.py
   ```

### Вакансии не отображаются в интерфейсе

1. **Обновите страницу** в браузере
2. **Проверьте фильтры** в интерфейсе
3. **Проверьте источник вакансий**
   - HeadHunter вакансии имеют `source: "headhunter"`
   - LinkedIn вакансии имеют `source: "linkedin"`

## 📝 Формат вакансий

Все вакансии сохраняются в едином формате:

```json
{
  "id": "уникальный-id",
  "title": "Название вакансии",
  "text": "Полное описание",
  "channel": "HeadHunter - Название компании",
  "link": "https://hh.ru/vacancy/...",
  "date": "2026-01-10T12:00:00",
  "source": "headhunter",
  "analysis": "HeadHunter: Python разработчик",
  "added_at": "2026-01-10T12:00:00",
  "is_new": true
}
```

## 🔧 Технические детали

### HeadHunter API

- **URL**: `https://api.hh.ru/vacancies`
- **Метод**: GET
- **Параметры**:
  - `text`: поисковый запрос
  - `area`: ID региона (1=Москва, 2=СПб, 113=Россия)
  - `per_page`: количество на странице (макс 100)
  - `page`: номер страницы
  - `period`: количество дней назад (макс 30)

### LinkedIn

- Использует Selenium WebDriver
- Открывает Chrome в headless режиме
- Парсит HTML страницы
- Может требовать авторизацию

## 📈 Производительность

- **HeadHunter**: ~50-100 вакансий в минуту
- **LinkedIn**: ~5-10 вакансий в минуту (зависит от скорости интернета)

## ⚠️ Ограничения

1. **HeadHunter API**:
   - Максимум 2000 страниц результатов
   - Рекомендуется не более 20 страниц за раз
   - Rate limiting: не более 100 запросов в минуту

2. **LinkedIn**:
   - Может блокировать автоматические запросы
   - Требует браузер (занимает ресурсы)
   - Медленнее, чем HeadHunter

## 💡 Рекомендации

1. **Для начала**: используйте только HeadHunter (быстрее и надежнее)
2. **Для LinkedIn**: используйте только при необходимости, с указанием email/password
3. **Количество страниц**: начните с 5-10 страниц для теста
4. **Фильтры**: используйте `keyword_filter` для отсеивания ненужных вакансий

## 🆘 Получение помощи

Если что-то не работает:

1. Запустите `python check_parsers.py` для диагностики
2. Запустите `python test_parsers.py` для тестирования парсеров
3. Проверьте логи в консоли при запуске `start_jobstalker.py`
4. Проверьте файл `data/settings.json` на корректность настроек
