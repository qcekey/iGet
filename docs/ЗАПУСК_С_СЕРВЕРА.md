# 🚀 Инструкция по запуску iGet с сервера

## 📍 Информация о сервере

- **IP адрес:** 85.198.84.197
- **Пользователь:** root
- **Пароль:** QV%LQ&dzXi9&
- **Директория проекта:** /opt/iget

---

## 🔌 Подключение к серверу

### Через SSH

```bash
ssh root@85.198.84.197
# При запросе введите пароль: QV%LQ&dzXi9&
```

### Через Windows PowerShell

```powershell
ssh root@85.198.84.197
```

---

## 🎯 Быстрый запуск приложения

### Вариант 1: Через systemd (рекомендуется)

Приложение уже настроено как системный сервис и запускается автоматически.

```bash
# Проверка статуса
systemctl status iget

# Запуск (если остановлено)
systemctl start iget

# Остановка
systemctl stop iget

# Перезапуск
systemctl restart iget

# Просмотр логов в реальном времени
journalctl -u iget -f

# Просмотр последних 50 строк логов
journalctl -u iget -n 50
```

### Вариант 2: Ручной запуск

```bash
# Подключитесь к серверу
ssh root@85.198.84.197

# Перейдите в директорию проекта
cd /opt/iget

# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите приложение
python start_jobstalker.py
```

Для остановки нажмите `Ctrl+C`.

---

## 🌐 Доступ к приложению

После запуска приложение будет доступно по адресам:

- **Через Nginx (рекомендуется):** http://85.198.84.197
- **Напрямую:** http://85.198.84.197:8000

Откройте любой из этих адресов в браузере.

---

## 📋 Основные команды управления

### Управление сервисом

```bash
# Статус сервиса
systemctl status iget

# Запуск
systemctl start iget

# Остановка
systemctl stop iget

# Перезапуск (при изменении кода)
systemctl restart iget

# Включение автозапуска при загрузке системы
systemctl enable iget

# Отключение автозапуска
systemctl disable iget
```

### Просмотр логов

```bash
# Логи в реальном времени (следить за работой)
journalctl -u iget -f

# Последние 100 строк
journalctl -u iget -n 100

# Логи за последний час
journalctl -u iget --since "1 hour ago"

# Логи за сегодня
journalctl -u iget --since today

# Логи с ошибками
journalctl -u iget -p err
```

### Проверка работы приложения

```bash
# Проверка, что порт 8000 открыт
netstat -tlnp | grep 8000

# Проверка через curl
curl http://localhost:8000

# Проверка процессов Python
ps aux | grep iget
```

---

## 🔧 Настройка приложения

### Редактирование настроек

```bash
# Подключитесь к серверу
ssh root@85.198.84.197

# Откройте файл настроек
nano /opt/iget/data/settings.json

# Или через vi
vi /opt/iget/data/settings.json
```

После изменения настроек перезапустите сервис:

```bash
systemctl restart iget
```

### Основные настройки в settings.json

```json
{
  "enable_headhunter": true,        // Включить парсинг HeadHunter
  "hh_search_query": "Python",      // Поисковый запрос
  "hh_area": 113,                    // Регион (113 = Россия)
  "hh_max_pages": 2,                 // Количество страниц
  
  "enable_linkedin": false,          // Включить парсинг LinkedIn
  "linkedin_search_query": "",       // Поисковый запрос для LinkedIn
  "linkedin_location": "",           // Локация
  
  "enable_telegram": false           // Включить парсинг Telegram
}
```

---

## 🔄 Обновление приложения

### Обновление кода

```bash
# 1. Остановите сервис
systemctl stop iget

# 2. Перейдите в директорию проекта
cd /opt/iget

# 3. Сделайте бэкап (рекомендуется)
cp -r data data_backup_$(date +%Y%m%d_%H%M%S)

# 4. Обновите код (через git, scp, rsync и т.д.)
# Например, если используете git:
# git pull

# 5. Обновите зависимости (если нужно)
source venv/bin/activate
pip install -r requirements_parsers.txt --upgrade

# 6. Запустите сервис
systemctl start iget

# 7. Проверьте статус
systemctl status iget
```

### Загрузка новых файлов с локального компьютера

**С вашего Windows компьютера:**

```powershell
# Через SCP
scp -r C:\Users\FreakNick\Desktop\iget-data\* root@85.198.84.197:/opt/iget/

# Через rsync (если установлен, например в Git Bash)
rsync -avz --exclude 'venv' --exclude '__pycache__' --exclude '*.pyc' `
  C:\Users\FreakNick\Desktop\iget-data\ root@85.198.84.197:/opt/iget/
```

После загрузки файлов перезапустите сервис:

```bash
systemctl restart iget
```

---

## 🐛 Решение проблем

### Приложение не запускается

```bash
# 1. Проверьте логи
journalctl -u iget -n 100

# 2. Проверьте, что все зависимости установлены
cd /opt/iget
source venv/bin/activate
python -c "import iget; print('OK')"

# 3. Проверьте права доступа
ls -la /opt/iget/data/
chmod 755 /opt/iget/data
```

### Не могу подключиться к приложению из браузера

```bash
# 1. Проверьте, что приложение запущено
systemctl status iget

# 2. Проверьте, что порт открыт
netstat -tlnp | grep 8000

# 3. Проверьте firewall
ufw status
# или
iptables -L -n

# 4. Проверьте Nginx (если используете)
systemctl status nginx
nginx -t
```

### Ошибки в логах

```bash
# Просмотр ошибок
journalctl -u iget -p err -n 50

# Поиск конкретной ошибки
journalctl -u iget | grep -i error
```

### Приложение падает и перезапускается

```bash
# Просмотр всех перезапусков
journalctl -u iget | grep -i restart

# Проверка использования ресурсов
htop
# или
free -h
df -h
```

---

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# CPU и память
htop
# или
top

# Дисковое пространство
df -h

# Использование памяти приложением
ps aux | grep iget
```

### Проверка работы парсеров

```bash
# Подключитесь к серверу
ssh root@85.198.84.197

# Перейдите в директорию проекта
cd /opt/iget

# Активируйте окружение
source venv/bin/activate

# Запустите диагностику
python check_parsers.py
```

---

## 🔒 Безопасность

### Рекомендации

1. **Измените пароль root** (если возможно):
   ```bash
   passwd
   ```

2. **Настройте SSH ключи** вместо пароля:
   ```bash
   # На вашем локальном компьютере
   ssh-keygen -t rsa
   ssh-copy-id root@85.198.84.197
   ```

3. **Настройте firewall** для ограничения доступа:
   ```bash
   ufw default deny incoming
   ufw default allow outgoing
   ufw allow ssh
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw enable
   ```

4. **Регулярно обновляйте систему**:
   ```bash
   apt update && apt upgrade -y
   ```

---

## 📝 Полезные команды

### Быстрая проверка статуса

```bash
# Одной командой проверить всё
echo "=== Статус сервиса ===" && \
systemctl status iget --no-pager -l && \
echo -e "\n=== Последние логи ===" && \
journalctl -u iget -n 20 --no-pager && \
echo -e "\n=== Порт 8000 ===" && \
netstat -tlnp | grep 8000
```

### Бэкап данных

```bash
# Создать бэкап
cd /opt/iget
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz data/

# Восстановить из бэкапа
tar -xzf backup_YYYYMMDD_HHMMSS.tar.gz
```

---

## ✅ Чек-лист первого запуска

- [ ] Подключение к серверу работает
- [ ] Приложение запущено: `systemctl status iget`
- [ ] Порт 8000 открыт: `netstat -tlnp | grep 8000`
- [ ] Приложение доступно: `curl http://localhost:8000`
- [ ] Nginx работает (если настроен): `systemctl status nginx`
- [ ] Приложение доступно из браузера: http://85.198.84.197
- [ ] Логи не показывают ошибок: `journalctl -u iget -n 50`

---

## 🎉 Готово!

Ваше приложение должно быть доступно по адресу **http://85.198.84.197**

Если возникли проблемы, проверьте раздел "Решение проблем" выше или логи приложения.
