# 🚀 Быстрый старт: HeadHunter и LinkedIn парсеры

## ✅ Что сделано

1. **HeadHunter парсер** - полностью рабочий через официальный API
2. **LinkedIn парсер** - работает через Selenium (требует Chrome)
3. **Интеграция с веб-интерфейсом** - кнопки включения/выключения
4. **Улучшенная обработка ошибок** - детальное логирование
5. **Диагностические скрипты** - для проверки работоспособности

## 📋 Как использовать

### Шаг 1: Включите HeadHunter

1. Запустите приложение: `python start_jobstalker.py`
2. Откройте веб-интерфейс в браузере
3. В разделе "BASIC SEARCH" найдите "Enable HeadHunter"
4. Включите переключатель
5. Укажите поисковый запрос (например: "Python разработчик")
6. Нажмите "Save Settings"

### Шаг 2: Проверьте работу

После сохранения настроек, при следующем запуске парсинга (кнопка "Start Search" или автоматический запуск), HeadHunter парсер автоматически получит вакансии.

### Шаг 3: Проверьте результаты

1. Откройте раздел "VACANCY DATABASE" в интерфейсе
2. Вакансии с HeadHunter будут отображаться с источником "HeadHunter - Название компании"
3. Или проверьте файл `data/vacancies.json`

## 🔍 Диагностика

Если что-то не работает, запустите:

```bash
python check_parsers.py
```

Этот скрипт покажет:
- ✅ Работает ли HeadHunter API
- ✅ Установлен ли Selenium для LinkedIn
- ✅ Правильно ли настроены парсеры
- ✅ Сохраняются ли вакансии

## ⚙️ Текущие настройки

Проверьте файл `data/settings.json`:

```json
{
  "enable_headhunter": true,    // Включен ли HeadHunter
  "hh_search_query": "разработчик",  // Поисковый запрос
  "hh_area": 1,                  // Регион (1=Москва, 2=СПб, 113=Россия)
  "hh_max_pages": 10,            // Количество страниц
  
  "enable_linkedin": false,      // Включен ли LinkedIn
  "linkedin_search_query": "...", // Поисковый запрос для LinkedIn
  "linkedin_location": "...",     // Локация для LinkedIn
  "linkedin_email": "",          // Email для авторизации (опционально)
  "linkedin_password": ""        // Password для авторизации (опционально)
}
```

## 🐛 Частые проблемы

### Проблема: HeadHunter не работает

**Решение:**
1. Проверьте, что `enable_headhunter: true` в `data/settings.json`
2. Убедитесь, что указан `hh_search_query`
3. Проверьте интернет-соединение
4. Запустите `python check_parsers.py` для диагностики

### Проблема: Вакансии не сохраняются

**Решение:**
1. Проверьте права доступа к папке `data/`
2. Запустите `python test_parsers_integration.py`
3. Проверьте логи в консоли при запуске приложения

### Проблема: Вакансии не отображаются в интерфейсе

**Решение:**
1. Обновите страницу в браузере (F5)
2. Проверьте фильтры в интерфейсе
3. Убедитесь, что вакансии есть в `data/vacancies.json`

## 📊 Статистика

После запуска парсинга, в консоли вы увидите:

```
============================================================
НАЧАЛО ПАРСИНГА ДОПОЛНИТЕЛЬНЫХ ИСТОЧНИКОВ
============================================================
Настройки парсеров:
  HeadHunter: enabled=True, query='разработчик'
  LinkedIn: enabled=False, query=''

============================================================
ПАРСИНГ HEADHUNTER
============================================================
HeadHunter: запрашиваем страницу 1/10
HeadHunter: получено 50 вакансий со страницы 1
HeadHunter: успешно распарсено 50 вакансий со страницы 1
...
HeadHunter: завершено, найдено 500 вакансий

============================================================
ПАРСИНГ ЗАВЕРШЕН. Всего найдено вакансий: 500
  HeadHunter: 500
  LinkedIn: 0
============================================================
```

## 💡 Советы

1. **Начните с малого**: установите `hh_max_pages: 1-2` для теста
2. **Используйте фильтры**: настройте `keyword_filter` для отсеивания ненужных вакансий
3. **Проверяйте логи**: все действия логируются с префиксом "HeadHunter:" или "LinkedIn:"
4. **LinkedIn медленный**: используйте только при необходимости, с указанием email/password

## 📚 Дополнительная информация

См. `PARSERS_USAGE.md` для подробной документации.
