# Независимое управление парсингом источников

## Что сделано

Теперь можно независимо включать/выключать парсинг с разных источников:
- **Telegram каналы** - можно включить/выключить отдельно
- **HeadHunter** - можно включить/выключить отдельно
- **LinkedIn** - можно включить/выключить отдельно

## Как использовать

### В веб-интерфейсе

1. Откройте раздел **"BASIC SEARCH"**
2. Вы увидите три переключателя:
   - **📱 TELEGRAM CHANNELS** - для включения/выключения парсинга Telegram каналов
   - **🎯 HEADHUNTER** - для включения/выключения парсинга HeadHunter
   - **🔗 LINKEDIN** - для включения/выключения парсинга LinkedIn

3. Включите нужные источники переключателями
4. Настройте параметры для каждого включенного источника
5. Нажмите **"Save Settings"** или просто переключите переключатель (настройки сохраняются автоматически)

### Примеры использования

#### Только HeadHunter
1. Выключите **TELEGRAM CHANNELS** (переключатель влево)
2. Включите **HEADHUNTER** (переключатель вправо)
3. Укажите поисковый запрос, регион и количество страниц
4. Запустите парсинг кнопкой **"Start Search"**

#### Только Telegram
1. Включите **TELEGRAM CHANNELS** (переключатель вправо)
2. Выключите **HEADHUNTER** и **LINKEDIN**
3. Добавьте каналы для мониторинга
4. Запустите парсинг

#### Комбинация источников
Можно включить любую комбинацию источников:
- Telegram + HeadHunter
- HeadHunter + LinkedIn
- Все три источника одновременно
- Или только один источник

## Технические детали

### Настройки в `data/settings.json`

Добавлено новое поле:
```json
{
  "enable_telegram": true,  // Включен ли парсинг Telegram каналов
  "enable_headhunter": true, // Включен ли парсинг HeadHunter
  "enable_linkedin": false  // Включен ли парсинг LinkedIn
}
```

### Логика работы

1. **Telegram парсинг**:
   - Запускается только если `enable_telegram: true`
   - Требует авторизации в Telegram
   - Требует указания каналов для мониторинга

2. **HeadHunter парсинг**:
   - Запускается только если `enable_headhunter: true`
   - Требует указания поискового запроса
   - Работает независимо от Telegram

3. **LinkedIn парсинг**:
   - Запускается только если `enable_linkedin: true`
   - Требует указания поискового запроса и локации
   - Работает независимо от других источников

### Порядок выполнения

При запуске парсинга (кнопка "Start Search"):
1. Сначала выполняется парсинг Telegram (если включен)
2. Затем выполняется парсинг HeadHunter (если включен)
3. Затем выполняется парсинг LinkedIn (если включен)

Каждый источник работает независимо и не блокирует другие.

## Важные замечания

1. **Telegram по умолчанию включен** - если вы хотите парсить только HeadHunter или LinkedIn, не забудьте выключить Telegram
2. **Настройки сохраняются автоматически** при изменении переключателей
3. **Можно включить все источники одновременно** - они будут выполняться последовательно
4. **Если источник выключен**, его парсинг просто пропускается без ошибок

## Примеры логов

### Только HeadHunter включен:
```
Telegram парсинг пропущен (отключен в настройках)
============================================================
НАЧАЛО ПАРСИНГА ДОПОЛНИТЕЛЬНЫХ ИСТОЧНИКОВ
============================================================
ПАРСИНГ HEADHUNTER
============================================================
HeadHunter: запрашиваем страницу 1/5
...
```

### Только Telegram включен:
```
Searching 7 days back in 1 channels
[1/1] OpenTo iGaming
Analyzing 10 messages...
Found 5 vacancies
Парсинг других источников завершен. Результаты: {'headhunter': 0, 'linkedin': 0, 'telegram': 0}
```

### Все источники включены:
```
Searching 7 days back in 1 channels
...
Found 5 vacancies
============================================================
ПАРСИНГ HEADHUNTER
============================================================
...
============================================================
ПАРСИНГ LINKEDIN
============================================================
...
```
