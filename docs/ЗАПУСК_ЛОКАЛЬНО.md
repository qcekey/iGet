# 🚀 Запуск iGet локально на Windows

## 📋 Быстрый старт

### Шаг 1: Активация виртуального окружения

Откройте PowerShell в папке проекта и выполните:

```powershell
cd c:\Users\FreakNick\Desktop\iget-data
.\venv\Scripts\Activate.ps1
```

Если возникнет ошибка о политике выполнения, выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Шаг 2: Установка зависимостей (если еще не установлены)

```powershell
pip install --upgrade pip
pip install -r requirements_parsers.txt
pip install fastapi uvicorn aiohttp selenium webdriver-manager beautifulsoup4 python-dotenv psutil jinja2
```

### Шаг 3: Запуск приложения

```powershell
python start_jobstalker.py
```

### Шаг 4: Открытие в браузере

После запуска откройте в браузере:
- **http://localhost:8000**

---

## ⚙️ Настройка переменных окружения (опционально)

Если нужны функции Telegram или AI, создайте файл `.env` в корне проекта:

```env
API_ID=ваш_api_id
API_HASH=ваш_api_hash
DEST_CHAT_ID=ваш_chat_id
GEMINI_API_KEY=ваш_ключ
GROQ_API_KEY=ваш_ключ
```

---

## 🔍 Проверка работы парсеров

Для проверки работы HeadHunter и LinkedIn парсеров:

```powershell
python check_parsers.py
```

---

## 🛑 Остановка приложения

Нажмите `Ctrl+C` в терминале, где запущено приложение.

---

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError"

**Решение:** Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements_parsers.txt
pip install fastapi uvicorn aiohttp selenium webdriver-manager beautifulsoup4 python-dotenv psutil jinja2
```

### Проблема: "Port 8000 is already in use"

**Решение:** 
1. Найдите процесс, использующий порт 8000:
   ```powershell
   netstat -ano | findstr :8000
   ```
2. Завершите процесс или измените порт в `iget/run.py`

### Проблема: "Cannot activate venv"

**Решение:** 
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 📝 Дополнительная информация

- **Настройки приложения:** `data/settings.json`
- **База вакансий:** `data/vacancies.json`
- **Логи:** выводятся в консоль при запуске

---

## 🎉 Готово!

После запуска приложение будет доступно по адресу **http://localhost:8000**
