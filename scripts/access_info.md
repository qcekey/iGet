# 🌐 Доступ к iGet и управление логами

## 📍 Доступ к веб-интерфейсу

Приложение доступно по адресам:

- **Через Nginx (рекомендуется):** http://85.198.84.197
- **Напрямую (порт 8000):** http://85.198.84.197:8000

Откройте любой из этих адресов в браузере.

---

## 📋 Просмотр логов

### Через SSH (на сервере)

```bash
# Подключение к серверу
ssh root@85.198.84.197
# Пароль: QV%LQ&dzXi9&

# Последние 50 строк логов
journalctl -u iget -n 50 --no-pager

# Логи в реальном времени (live)
journalctl -u iget -f

# Логи с определенного времени
journalctl -u iget --since "1 hour ago"

# Логи за сегодня
journalctl -u iget --since today

# Логи с ошибками
journalctl -u iget -p err
```

### С локального компьютера (через SSH)

```bash
# Последние логи
ssh root@85.198.84.197 "journalctl -u iget -n 50 --no-pager"

# Логи в реальном времени
ssh root@85.198.84.197 "journalctl -u iget -f"
```

---

## 🔧 Управление сервисом

```bash
# Статус сервиса
systemctl status iget

# Запуск
systemctl start iget

# Остановка
systemctl stop iget

# Перезапуск
systemctl restart iget

# Просмотр логов
journalctl -u iget -f
```

---

## 🐛 Диагностика проблем

```bash
# Проверка, что сервис запущен
systemctl is-active iget

# Проверка портов
netstat -tlnp | grep 8000
# или
ss -tlnp | grep 8000

# Проверка доступности изнутри сервера
curl http://localhost:8000

# Проверка Nginx
systemctl status nginx
nginx -t
```

---

## 📊 Полезные команды

```bash
# Все логи с временными метками
journalctl -u iget --no-pager

# Логи с фильтром по ключевому слову
journalctl -u iget | grep "ERROR"
journalctl -u iget | grep "Traceback"

# Экспорт логов в файл
journalctl -u iget --since "1 hour ago" > /tmp/iget_logs.txt

# Размер логов
journalctl -u iget --disk-usage
```
