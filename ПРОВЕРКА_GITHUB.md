# ✅ Чеклист проверки рабочей версии на GitHub

## 🔍 Как проверить, что на GitHub загружена рабочая версия

### Шаг 1: Откройте репозиторий на GitHub

Перейдите: **https://github.com/qcekey/iGet**

---

## ✅ Проверка 1: Основные файлы проекта

Убедитесь, что видны следующие файлы и папки:

- ✅ `README.md` - главный файл документации
- ✅ `LICENSE` - файл лицензии MIT
- ✅ `CONTRIBUTING.md` - руководство для разработчиков
- ✅ `.gitignore` - файл исключений
- ✅ `.env.example` - пример конфигурации
- ✅ `requirements_parsers.txt` - зависимости проекта
- ✅ `start_iget.py` - точка входа
- ✅ Папка `iget/` - основной модуль
- ✅ Папка `parsers/` - парсеры
- ✅ Папка `scripts/` - скрипты
- ✅ Папка `tests/` - тесты
- ✅ Папка `docs/` - документация
- ✅ Папка `static/` - веб-интерфейс

---

## 🔒 Проверка 2: Чувствительные файлы НЕ должны быть видны

**КРИТИЧЕСКИ ВАЖНО:** Следующие файлы НЕ должны быть видны на GitHub:

- ❌ `.env` - **НЕ должен быть виден**
- ❌ `keyserver` - **НЕ должен быть виден**
- ❌ `keyserver.pub` - **НЕ должен быть виден**
- ❌ `data/settings.json` - **НЕ должен быть виден**
- ❌ `data/*.session` - **НЕ должны быть видны**
- ❌ `data/*.db` - **НЕ должны быть видны**
- ❌ `data/vacancies.json` - **НЕ должен быть виден**
- ❌ `data/tracker.json` - **НЕ должен быть виден**

**Как проверить:**
1. На GitHub нажмите на кнопку "Go to file" или просто прокрутите список файлов
2. Попробуйте найти эти файлы через поиск (Ctrl+F)
3. Если они видны - **это проблема безопасности!**

---

## 📋 Проверка 3: README.md отображается правильно

1. На главной странице репозитория должен отображаться README.md
2. Проверьте, что:
   - ✅ Есть описание проекта
   - ✅ Есть инструкции по установке
   - ✅ Есть ссылки на документацию
   - ✅ Ссылки на Issues правильные: `https://github.com/qcekey/iGet/issues`

---

## 📁 Проверка 4: Структура проекта

Проверьте основные папки:

### `iget/` - основной модуль
Должны быть файлы:
- `main.py`
- `web_ui.py`
- `ai_client.py`
- `ml_filter.py`
- `config.py`
- `models.py`
- и другие...

### `parsers/` - парсеры
Должны быть:
- `base_parser.py`
- `headhunter_parser.py`
- `linkedin_parser.py`

### `docs/` - документация
Должны быть файлы документации (могут быть на русском)

---

## 🔗 Проверка 5: Ссылки работают

1. Нажмите на файл `README.md`
2. Проверьте, что ссылки кликабельны:
   - Ссылки на документацию
   - Ссылки на Issues
   - Ссылки на другие файлы

---

## 🧪 Проверка 6: Можно клонировать репозиторий

Проверьте, что репозиторий можно клонировать:

```bash
# Создайте временную папку и попробуйте клонировать
cd C:\temp
git clone https://github.com/qcekey/iGet.git test-clone
cd test-clone

# Проверьте, что файлы есть
dir

# Удалите тестовую папку
cd ..
rmdir /s test-clone
```

---

## 📊 Проверка 7: История коммитов

На GitHub:
1. Перейдите на вкладку **"Commits"**
2. Должны быть видны коммиты:
   - "Initial commit: iGet vacancy monitoring system"
   - "Merge remote and local repositories"
   - "Remove sensitive files (.env, keyserver) from repository and update .gitignore"

---

## ⚠️ Если что-то не так

### Проблема: Чувствительные файлы видны на GitHub

**СРОЧНО:**
1. Если `.env` или `keyserver` видны на GitHub:
   - Смените все API ключи и пароли
   - Удалите файлы из истории (уже сделано)
   - Проверьте, что они больше не видны

2. Проверьте историю коммитов - старые коммиты могут содержать эти файлы

### Проблема: Файлы не загружены

Если каких-то файлов не хватает:
1. Проверьте локально: `git status`
2. Добавьте недостающие файлы: `git add .`
3. Закоммитьте: `git commit -m "Add missing files"`
4. Загрузите: `git push origin main`

---

## ✅ Итоговый чеклист

- [ ] Все основные файлы проекта на месте
- [ ] `.env` файл **НЕ виден** на GitHub
- [ ] `keyserver` файлы **НЕ видны** на GitHub
- [ ] Папка `data/` **НЕ видна** на GitHub
- [ ] README.md отображается правильно
- [ ] Структура проекта корректна
- [ ] Ссылки в README работают
- [ ] Репозиторий можно клонировать
- [ ] История коммитов видна

---

## 🎉 Если все проверки пройдены

Поздравляем! Ваш проект готов к совместной разработке на GitHub! 🚀

**Ссылка на репозиторий:** https://github.com/qcekey/iGet

---

## 📝 Дополнительные проверки (опционально)

### Проверка через GitHub API

Можно проверить через API:

```powershell
# Проверьте список файлов через API
Invoke-RestMethod -Uri "https://api.github.com/repos/qcekey/iGet/contents" | Select-Object name, type
```

### Проверка через Git

```powershell
# Проверьте, что отслеживается Git
git ls-files | Select-String -Pattern "\.env|keyserver"

# Должно быть пусто (ничего не выводится)
```

---

**Дата проверки:** _______________
**Проверил:** _______________
**Статус:** ✅ / ❌
